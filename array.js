 var array_caounter = function(array){
    return "there is " + array.length + " elements in array"
 };

var multiply = function (x,y) {
    return `$(x) multiply $(y) = $(x*y)`;
}

var some_value = 451;
module.exports = array_caounter;